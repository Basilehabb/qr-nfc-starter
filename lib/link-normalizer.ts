export type NormalizedLink = { label: string; href: string }; export function normalizeLinks(p:{whatsappNumber?:string;instagram?:string;facebook?:string;website?:string;}):NormalizedLink[]{const a:NormalizedLink[]=[];if(p.whatsappNumber){const n=p.whatsappNumber.replace(/[^0-9]/g,'');if(n)a.push({label:'WhatsApp',href:`https://wa.me/${n}`});}if(p.instagram){const h=p.instagram.replace(/^@/,'');if(h)a.push({label:'Instagram',href:`https://instagram.com/${h}`});}if(p.facebook){const f=p.facebook.replace(/^@/,'');if(f)a.push({label:'Facebook',href:`https://facebook.com/${f}`});}if(p.website&&/^https:\/\//.test(p.website)){a.push({label:'Website',href:p.website});}return a;}
