import { notFound } from 'next/navigation';type Props={params:{slug:string}};export default function Page({params}:Props){const {slug}=params;const mock=slug==='demo'?{displayName:'Basil Ehab',avatarUrl:'https://i.pravatar.cc/150?img=12',bio:'لو لقيت الحاجة دي، شكرًا! تواصل معايا مباشرة.',whatsapp:'201234567890',instagram:'basilehab',facebook:'basilehab',website:'https://example.com'}:null;if(!mock)return notFound();const links=[{label:'WhatsApp',href:`https://wa.me/${mock.whatsapp}`},{label:'Instagram',href:`https://instagram.com/${mock.instagram}`},{label:'Facebook',href:`https://facebook.com/${mock.facebook}`},{label:'Website',href:mock.website},];return(<main style={{padding:24}}><img src={mock.avatarUrl} alt={mock.displayName} width={96} height={96}/><h2>{mock.displayName}</h2><p>{mock.bio}</p><ul>{links.map(l=> <li key={l.label}><a href={l.href} target='_blank' rel='noreferrer'>{l.label}</a></li>)}</ul></main>);}